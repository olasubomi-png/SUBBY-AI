import { trpc } from "@/lib/trpc";
import { AlertCircle, CheckCircle2, ChevronRight, CircleDot, FileCode2, GitPullRequest, Github, Link2, Loader2, LogOut, Play, ShieldCheck, Unplug, Wand2 } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { toast } from "sonner";
import { useLocation } from "wouter";

export default function GitHubWorkspace() {
  const [, setLocation] = useLocation();
  const { data: overview } = trpc.workspace.overview.useQuery();
  const projects = overview?.projects ?? [];
  const [selectedProjectId, setSelectedProjectId] = useState<number | null>(null);
  const projectId = selectedProjectId ?? projects[0]?.id ?? null;
  const utils = trpc.useUtils();
  const status = trpc.github.status.useQuery();
  useEffect(() => {
    const params = new URLSearchParams(window.location.search);
    if (params.get("connected") === "1") {
      toast.success("GitHub account connected securely.");
      window.history.replaceState(null, "", "/github");
    }
    const error = params.get("github_error");
    if (error) {
      toast.error(error);
      window.history.replaceState(null, "", "/github");
    }
  }, []);
  const repos = trpc.github.listRepositories.useQuery(undefined, { enabled: Boolean(status.data?.connection) });
  const [repositoryName, setRepositoryName] = useState("");
  const context = trpc.github.repositoryContext.useQuery({ projectId: projectId ?? 1 }, { enabled: Boolean(projectId && status.data?.connection) });
  const [selectedPath, setSelectedPath] = useState("");
  const [changeInstruction, setChangeInstruction] = useState("");
  const [workflowId, setWorkflowId] = useState("");
  const file = trpc.github.readRepositoryFile.useQuery({ projectId: projectId ?? 1, path: selectedPath }, { enabled: Boolean(projectId && selectedPath) });
  const connect = trpc.github.startConnection.useMutation({ onSuccess: ({ url }) => window.location.assign(url), onError: (error) => toast.error(error.message) });
  const bind = trpc.github.bindRepository.useMutation({ onSuccess: async () => { if (projectId) await utils.github.repositoryContext.invalidate({ projectId }); toast.success("Repository linked to this project."); }, onError: (error) => toast.error(error.message) });
  const disconnect = trpc.github.disconnect.useMutation({ onSuccess: async () => { await utils.github.status.invalidate(); toast.success("GitHub connection removed from SUBBY."); }, onError: (error) => toast.error(error.message) });
  const review = trpc.github.inspectFile.useMutation({ onError: (error) => toast.error(error.message) });
  const proposal = trpc.github.proposeFileFix.useMutation({ onError: (error) => toast.error(error.message) });
  const workflows = trpc.github.listWorkflows.useQuery({ projectId: projectId ?? 1 }, { enabled: Boolean(projectId && context.data?.repository) });
  const history = trpc.github.operationHistory.useQuery({ projectId: projectId ?? 1 }, { enabled: Boolean(projectId && status.data?.connection) });
  const dispatch = trpc.github.dispatchWorkflow.useMutation({ onSuccess: () => toast.success("GitHub Actions workflow dispatched."), onError: (error) => toast.error(error.message) });
  const createPull = trpc.github.createPullRequest.useMutation({ onSuccess: (result) => { toast.success(`Pull request #${result.number} created.`); window.open(result.url, "_blank", "noopener,noreferrer"); }, onError: (error) => toast.error(error.message) });
  const selectedRepo = useMemo(() => repos.data?.find((repo) => repo.fullName === repositoryName), [repos.data, repositoryName]);

  if (!projects.length) return <section className="github-no-project"><Github className="size-8" /><p className="eyebrow">GITHUB WORKSPACE</p><h1>Create a project before connecting a repository.</h1><p>Repository access is purpose-bound: connect an account, then explicitly link one of its repositories to a SUBBY project.</p><button className="subby-primary-button" onClick={() => setLocation("/projects")}>Open project hub <ChevronRight className="size-4" /></button></section>;

  if (!status.data?.configured) return <section className="github-setup-page"><div className="github-setup-icon"><AlertCircle className="size-6" /></div><p className="eyebrow">GITHUB SETUP</p><h1>GitHub OAuth is not configured yet.</h1><p>The workspace connection cannot begin until the server has a verified GitHub OAuth client ID and client secret. These remain server-side and are never displayed in the app.</p></section>;

  return <section className="github-workspace-page"><div className="github-heading"><div><p className="eyebrow">GITHUB WORKSPACE</p><h1>Connect code context with clear control.</h1><p>SUBBY reads only repositories you explicitly link to a project. Your GitHub token is encrypted on the server and never sent to the browser.</p></div><div className="github-heading-icon"><Github className="size-5" /></div></div><div className="github-project-picker"><span>Project</span><select value={projectId ?? ""} onChange={(event) => { setSelectedProjectId(Number(event.target.value)); setSelectedPath(""); }}>{projects.map((project) => <option value={project.id} key={project.id}>{project.name}</option>)}</select>{status.data.connection ? <span className="github-connected-label"><CheckCircle2 className="size-3.5" /> @{status.data.connection.githubLogin} connected</span> : <span className="github-disconnected-label"><Unplug className="size-3.5" /> Not connected</span>}</div>
    {!status.data.connection ? <section className="github-connect-card"><div><ShieldCheck className="size-6" /><h2>Connect your GitHub account</h2><p>You will review GitHub’s authorization screen. SUBBY requests repository read/write and workflow access solely to support the project features you approve.</p><div className="github-scope-list"><span>Repository selection</span><span>Read code context</span><span>Workflow dispatch</span><span>Approved change push</span></div></div><button className="subby-primary-button" disabled={connect.isPending} onClick={() => connect.mutate()}>{connect.isPending ? <><Loader2 className="size-4 animate-spin" /> Opening GitHub…</> : <><Link2 className="size-4" /> Connect GitHub</>}</button></section> : <><div className="github-layout"><section className="subby-panel github-repository-panel"><div className="panel-heading"><div><p className="eyebrow">PROJECT REPOSITORY</p><h2>Link a repository</h2></div><button className="github-disconnect" onClick={() => { if (window.confirm("Disconnect GitHub and remove all project repository links?")) disconnect.mutate({ confirmed: true }); }}><LogOut className="size-3.5" /> Disconnect</button></div><div className="github-repository-body"><label>Select repository<select value={repositoryName || context.data?.repository?.fullName || ""} onChange={(event) => setRepositoryName(event.target.value)}><option value="">Choose from your GitHub account</option>{repos.data?.map((repo) => <option value={repo.fullName} key={repo.id}>{repo.fullName}{repo.private ? " · private" : ""}</option>)}</select></label><button className="subby-primary-button w-full" disabled={!selectedRepo || bind.isPending} onClick={() => { if (projectId && repositoryName) bind.mutate({ projectId, fullName: repositoryName }); }}>{bind.isPending ? "Linking repository…" : "Link repository to project"}<Link2 className="size-4" /></button>{context.data?.repository && <div className="github-linked-repo"><CircleDot className="size-4" /><div><strong>{context.data.repository.fullName}</strong><span>{context.data.repository.private ? "Private repository" : "Public repository"} · {context.data.repository.defaultBranch}</span></div></div>}</div></section><section className="subby-panel github-files-panel"><div className="panel-heading"><div><p className="eyebrow">READ-ONLY INSPECTION</p><h2>Repository files</h2></div><span className="record-count">{context.data?.files.length ?? 0}</span></div>{context.isLoading ? <div className="tool-loading"><Loader2 className="size-4 animate-spin" /> Reading repository tree…</div> : context.data?.repository ? <div className="github-file-browser"><div className="github-file-list">{context.data.files.map((path) => <button className={selectedPath === path ? "selected" : ""} key={path} onClick={() => setSelectedPath(path)}><FileCode2 className="size-3.5" /> {path}</button>)}</div><div className="github-file-preview">{file.isLoading ? <div className="tool-loading"><Loader2 className="size-4 animate-spin" /> Loading file…</div> : file.data ? <><header>{file.data.path}</header><pre>{file.data.content}</pre></> : <div className="github-preview-empty"><FileCode2 className="size-6" /><p>Select a source file to inspect its contents.</p></div>}</div></div> : <div className="tool-empty"><p>No repository linked to this project</p><span>Select a repository from your connected GitHub account to give SUBBY read-only code context.</span></div>}</section></div>{context.data?.repository && <section className="subby-panel github-action-panel"><div className="panel-heading"><div><p className="eyebrow">SUBBY REPOSITORY ASSIST</p><h2>Review, propose, test, then approve</h2></div><Wand2 className="size-4 text-cyan-200" /></div><div className="github-action-grid"><div className="github-ai-action"><p>1. Inspect a selected file</p><button disabled={!selectedPath || review.isPending} onClick={() => projectId && review.mutate({ projectId, path: selectedPath })}><Wand2 className="size-3.5" /> {review.isPending ? "Reviewing…" : "Ask SUBBY to review"}</button>{review.data && <pre>{review.data.review}</pre>}</div><div className="github-ai-action"><p>2. Propose a targeted change</p><textarea value={changeInstruction} onChange={(event) => setChangeInstruction(event.target.value)} placeholder="Describe the fix or improvement you want in the selected file." /><button disabled={!selectedPath || changeInstruction.trim().length < 6 || proposal.isPending} onClick={() => projectId && proposal.mutate({ projectId, path: selectedPath, instruction: changeInstruction })}><Wand2 className="size-3.5" /> {proposal.isPending ? "Preparing proposal…" : "Create change proposal"}</button>{proposal.data && <div className="github-proposal"><strong>{proposal.data.summary}</strong><span>{proposal.data.commitMessage}</span><button className="github-pr-button" disabled={createPull.isPending} onClick={() => { if (window.confirm("Create a new branch, commit the proposed full-file change, and open a pull request on GitHub?")) projectId && createPull.mutate({ projectId, path: proposal.data.path, content: proposal.data.content, commitMessage: proposal.data.commitMessage, summary: proposal.data.summary, confirmed: true }); }}><GitPullRequest className="size-3.5" /> {createPull.isPending ? "Creating pull request…" : "Approve & create pull request"}</button></div>}</div><div className="github-ai-action"><p>3. Run an existing GitHub Actions workflow</p><select value={workflowId} onChange={(event) => setWorkflowId(event.target.value)}><option value="">Select a workflow</option>{workflows.data?.filter((flow) => flow.state === "active").map((flow) => <option value={flow.id} key={flow.id}>{flow.name}</option>)}</select><button disabled={!workflowId || dispatch.isPending} onClick={() => { if (window.confirm("Dispatch this existing GitHub Actions workflow on the repository default branch?")) projectId && dispatch.mutate({ projectId, workflowId: Number(workflowId), confirmed: true }); }}><Play className="size-3.5" /> {dispatch.isPending ? "Dispatching…" : "Approve & run workflow"}</button><small>SUBBY dispatches only an existing workflow; it does not run untrusted repository code on this app server.</small></div></div></section>}<section className="subby-panel github-history-panel"><div className="panel-heading"><div><p className="eyebrow">OPERATION HISTORY</p><h2>Repository activity</h2></div><span className="record-count">{history.data?.length ?? 0}</span></div>{history.data?.length ? <div className="github-history-list">{history.data.slice(0, 8).map((entry) => <div key={entry.id}><CircleDot className="size-3" /><span><strong>{entry.title}</strong><small>{entry.detail || "GitHub workspace"}</small></span><time>{new Date(entry.createdAt).toLocaleString()}</time></div>)}</div> : <div className="tool-empty"><p>No repository operations yet</p><span>Connection, review, test, and pull-request events will be recorded here after you begin working with a linked repository.</span></div>}</section><div className="github-safety-note"><ShieldCheck className="size-4" /><span>Repository content is read only until you approve a separate action. Pull requests are created on a new `subby/` branch; workflows are dispatched only after your confirmation.</span></div></>}
  </section>;
}
